[project]
name = "backend"
version = "0.1.0"
description = ""
authors = [
    {name = "Andrew Krylov",email = "89921337+andreynes@users.noreply.github.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi (>=0.115.12,<0.116.0)",
    "uvicorn[standard] (>=0.34.2,<0.35.0)",
    "pydantic-settings (>=2.9.1,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

# >>> ДОБАВЬТЕ ЭТУ СЕКЦИЮ <<<
[tool.poetry]
name = "backend"
version = "0.1.0"
description = ""
authors = ["Andrew Krylov <89921337+andreynes@users.noreply.github.com>"]
readme = "README.md"
packages = [{include = "app", from = "."}] # Пример, если ваш код в папке app

[tool.poetry.dependencies]
python = ">=3.10"
fastapi = ">=0.115.12,<0.116.0"
uvicorn = {extras = ["standard"], version = ">=0.34.2,<0.35.0"}

# Если у вас есть dev-зависимости, они должны быть в [tool.poetry.group.dev.dependencies]
# [tool.poetry.group.dev.dependencies]
# pytest = "^7.0"